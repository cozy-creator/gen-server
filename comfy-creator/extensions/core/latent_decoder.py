# This should switch from non-tiled to tiled mode if there is not enough VRAM
# available to decode the entire image at once.
# There should be an advanced setting toggle that can be used to turn off the auto-tiling
# behavior.
