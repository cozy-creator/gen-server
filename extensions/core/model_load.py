# If we do have a general model-loader, it will need to support:
# denoiser built-in (Unet, VAE, text-encoder)
# Individual denoiser components (Unet, VAE, text-encoder / CLIP)
# LoRa's
# GLIGEN
# Clip Vision
# controlnet
